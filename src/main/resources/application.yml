server:
  port: 8081
  servlet:
    context-path: /api/barbershop
spring:
  application:
    name: ms-scheduling
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ms_scheduling?createDatabaseIfNotExist=true
    username: root
    password: michelflava
  jpa:
    hibernate:
      ddl-auto: update
    generate-ddl: true
    show-sql: true
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    operations-sorter: method
    path: /swagger-ui.html
